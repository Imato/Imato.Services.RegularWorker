{
  "ConnectionStrings": {
    "postgres": "Host=localhost;Port=5432;Database=unit_tests;Pooling=true;MinPoolSize=10;MaxPoolSize=100;User ID={UNIT_TESTS_USER};Password={UNIT_TESTS_PASSWORD};"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Debug"
    },
    "DbLogger": {
      "Options": {
        "ConnectionString": "Host=localhost;Port=5432;Database=unit_tests;Pooling=true;MinPoolSize=10;MaxPoolSize=100;User ID={UNIT_TESTS_USER};Password={UNIT_TESTS_PASSWORD};",
        "Table": "log.logs",
        "Columns": "app,dt,level,message,host_name,source"
      }
    }
  }
}